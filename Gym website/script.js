document.querySelectorAll(".plan button").forEach(button => {
    button.addEventListener("click", () => {
        alert("Thanks for your interest! Please contact us to join PowerFit Gym.");
    });
});
